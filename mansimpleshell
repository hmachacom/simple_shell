.\" Automatically generated by Pandoc 2.5
.\"
.TH "" "" "" "" ""
.hy
.SS Holberton Shell 3 \[lq]7 Diciembre 2021\[rq] \[lq]hsh man page\[rq]
.SH NAME
.IP
.nf
\f[C]
hsh \- Simple Shell Program (command\-line interpreter)
\f[R]
.fi
.SH SYNOPSIS
.IP
.nf
\f[C]
hsh

#include <stdarg.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <dirent.h>
#include <errno.h>
#include <fcntl.h>
#include <signal.h>
#include <sys/resource.h>
#include <sys/stat.h>
#include <sys/time.h>
#include <sys/types.h>
#include <sys/wait.h>
#ifndef SHELL_H
#define SHELL_H
#endif
extern char **environ;
\f[R]
.fi
.SS To compile use:
.IP
.nf
\f[C]
\[dq]gcc \-Wall \-Werror \-Wextra \-pedantic \-std=gnu89 *.c \-o hsh\[dq]
\f[R]
.fi
.SH DESCRIPTION
.PP
This function is how a simple shell, is a basic imitacion of the
standard command interpreter for the system.
This shell is a command that reads lines from either a file or the
terminal, interprets them, and generally executes other commands (exit
and env).
Also, it allows you to receive interactive and non\-interactive inputs
(shell scripts).
That is, the commands can be written directly to the running shell or
they can be placed in a file and the file can be run directly in the
shell.
Finally, it shows errors according to the input and count the number of
searchings that did the user just as the shell does.
.SH BUILT\-IN COMMANDS
.SS Exit
.IP
.nf
\f[C]
Exit the shell (similar to \[dq]ctrl + d\[dq] or write \[dq]exit\[dq])
\f[R]
.fi
.SS Env
.IP
.nf
\f[C]
Prints the environment variables
\f[R]
.fi
.SH EXAMPLES
.SS Inputs
.SS Interactive mode
.PP
# ls \-la
.PP
# /bin/ls
.PP
# pwd
.PP
# cat text_file
.SS Inputs
.SS Non\-Interactive mode
.PP
$ echo \[lq]/bin/ls\[rq] | ./hsh
.PP
$ cat test_ls_2 | ./hsh
.PP
$ echo \[lq]aejkf\[rq] | ./hsh // error
.SH EXAMPLE OF ERRORS
.PP
# ls k ls: cannot access `k': No such file or directory
.PP
# QUERTY sh : 2: QUERTY: not found
.SH SEE ALSO
.IP
.nf
\f[C]
1. prompt.c
2. shell.h
3. searchDir.c
4.exec.c
5.hijo.c
6.path.c
7.fresspp.c
8. _strtok.c
Full documentation at: <https://github.com/hmachacom/simple_shell>
\f[R]
.fi
.SH BUGS
.IP
.nf
\f[C]
No bugs.
\f[R]
.fi
.SH NOTES
.IP
.nf
\f[C]
The files and code were creates and execute by Ubuntu 20.04 LTS using the editor Visual Studio Code
\f[R]
.fi
.SH AUTHOR
.IP
.nf
\f[C]
Written by Carlos Pardo and Hugo Machacon, Cohort 16.
\f[R]
.fi
.SH EMAILS
.IP \[bu] 2
Carlos Andres Pardo 
.IP \[bu] 2
Hugo Machacon
.SH COPYRIGHT
.IP
.nf
\f[C]
Copyright\[at]2021 Holberton School, student project with review free.
\[dq]finally shell\[dq]
\f[R]
.fi